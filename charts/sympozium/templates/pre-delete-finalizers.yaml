{{- if .Values.rbac.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sympozium-pre-delete
  namespace: {{ include "sympozium.namespace" . }}
  labels:
    {{- include "sympozium.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-delete
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded,hook-failed
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "sympozium.fullname" . }}-pre-delete
  labels:
    {{- include "sympozium.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-delete
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded,hook-failed
rules:
  - apiGroups: ["sympozium.ai"]
    resources:
      - skillpacks
      - ensembles
      - agents
      - agentruns
      - sympoziumpolicies
      - sympoziumschedules
      - sympoziumconfigs
    verbs: ["get", "list", "patch"]
  - apiGroups: ["sympozium.ai"]
    resources:
      - skillpacks/finalizers
      - ensembles/finalizers
      - agents/finalizers
      - sympoziumpolicies/finalizers
      - sympoziumschedules/finalizers
      - sympoziumconfigs/finalizers
    verbs: ["update"]
  - apiGroups: ["apps"]
    resources:
      - deployments
      - deployments/scale
    verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "sympozium.fullname" . }}-pre-delete
  labels:
    {{- include "sympozium.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-delete
    "helm.sh/hook-weight": "-10"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded,hook-failed
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "sympozium.fullname" . }}-pre-delete
subjects:
  - kind: ServiceAccount
    name: sympozium-pre-delete
    namespace: {{ include "sympozium.namespace" . }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: sympozium-pre-delete-finalizers
  namespace: {{ include "sympozium.namespace" . }}
  labels:
    {{- include "sympozium.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-delete
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded,hook-failed
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 60
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sympozium-pre-delete
    spec:
      serviceAccountName: sympozium-pre-delete
      restartPolicy: Never
      containers:
        - name: strip-finalizers
          image: soldevelo/kubectl:1.36
          command:
            - /bin/sh
            - -c
            - |
              set -e
              NAMESPACE="{{ include "sympozium.namespace" . }}"

              {{- if .Values.celln.nativeParent.enabled }}
              # Refuse BEFORE scaling controllers or stripping anything. A failed
              # read is not evidence of teardown. The operator must close new
              # admission, delete runs normally and await joined cleanup first.
              parent_runs=$(kubectl get agentruns.sympozium.ai -n {{ .Values.celln.nativeParent.namespace | quote }} -o jsonpath='{range .items[*]}{.metadata.name}{"\n"}{end}')
              if [ -n "$parent_runs" ]; then
                echo "Refusing uninstall: delete native parent runs and wait for confirmed cleanup first." >&2
                exit 1
              fi
              {{- end }}

              # Scale down the controller first so it cannot re-add finalizers
              echo "Scaling down controller deployment..."
              kubectl scale deployment sympozium-controller-manager -n "$NAMESPACE" --replicas=0 2>/dev/null || true
              kubectl rollout status deployment/sympozium-controller-manager -n "$NAMESPACE" --timeout=60s 2>/dev/null || true

              # AgentRun finalizers own real execution cleanup (including host
              # cells); stripping them is never proof that execution stopped.
              RESOURCES="skillpacks.sympozium.ai ensembles.sympozium.ai agents.sympozium.ai sympoziumpolicies.sympozium.ai sympoziumschedules.sympozium.ai sympoziumconfigs.sympozium.ai"
              for res in $RESOURCES; do
                echo "Stripping finalizers from $res..."
                items=$(kubectl get "$res" --all-namespaces -o jsonpath='{range .items[*]}{.metadata.namespace}/{.metadata.name}{"\n"}{end}' 2>/dev/null || true)
                for item in $items; do
                  [ -z "$item" ] && continue
                  ns="${item%%/*}"
                  name="${item#*/}"
                  echo "  Patching $res $ns/$name"
                  kubectl patch "$res" "$name" -n "$ns" --type=merge -p '{"metadata":{"finalizers":[]}}' 2>/dev/null || true
                done
              done
              echo "Done stripping finalizers."
{{- end }}
