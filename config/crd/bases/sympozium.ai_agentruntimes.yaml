---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: agentruntimes.sympozium.ai
spec:
  group: sympozium.ai
  names:
    kind: AgentRuntime
    listKind: AgentRuntimeList
    plural: agentruntimes
    singular: agentruntime
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.resolvedImageDigest
      name: Digest
      type: string
    - jsonPath: .spec.contractVersion
      name: Contract
      type: string
    - jsonPath: .spec.supportOwner
      name: Owner
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AgentRuntime describes an administrator-approved runtime that replaces
          Sympozium's built-in `agent-runner` as the pod's primary process.

          It is the object-form, admin-owned counterpart to the inline `task.mode:
          harness` parameters: instead of a run author naming an image and a set of
          capability claims per run, they reference a runtime the platform team has
          vetted. The resource pins the digest, declares the adapter contract and
          capabilities, and records a support owner so a run can show provenance.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AgentRuntimeSpec is the desired state of an AgentRuntime. Existing inline
              Celln authority remains supported and namespaced. New shared-catalogue use is
              explicit through cellnProfileRef and may only narrow the selected platform
              profile through cellnLimits.
            properties:
              capabilities:
                description: |-
                  Capabilities declares what this runtime honours, from the same set the
                  harness task mode accepts (persona, toolFilter). Empty means it claims
                  nothing.
                items:
                  type: string
                type: array
              celln:
                description: |-
                  Celln declares an additional placement profile, not executable authority.
                  A Celln-native runtime (no OCI adapter) may set this with an empty image;
                  OCI runtimes still require image. OCI Ready never implies CellnReady.
                properties:
                  closure:
                    properties:
                      hash:
                        maxLength: 71
                        pattern: ^blake3:[0-9a-f]{64}$
                        type: string
                    required:
                    - hash
                    type: object
                  contractVersion:
                    enum:
                    - celln.reference-functions/v1
                    - celln.json-tools/v1
                    type: string
                  entryPoint:
                    maxLength: 256
                    pattern: ^/([A-Za-z0-9_-][A-Za-z0-9_.+-]*/)*[A-Za-z0-9_-][A-Za-z0-9_.+-]*$
                    type: string
                  executable:
                    properties:
                      hash:
                        maxLength: 71
                        pattern: ^blake3:[0-9a-f]{64}$
                        type: string
                    required:
                    - hash
                    type: object
                  json:
                    description: CellnHarnessJSONLimits are explicit bounded loop
                      ceilings, not model grants.
                    properties:
                      maxCalls:
                        format: int64
                        maximum: 16
                        minimum: 0
                        type: integer
                      maxTurns:
                        format: int64
                        maximum: 6
                        minimum: 1
                        type: integer
                    required:
                    - maxCalls
                    - maxTurns
                    type: object
                  lane:
                    enum:
                    - agent
                    type: string
                  lifecycle:
                    enum:
                    - disposable-one-shot
                    - enduring
                    type: string
                  limits:
                    description: |-
                      AgentRuntimeCellnLimits are ceilings, never Agent grants or provider policy.
                      Model mediation and tools retain their selected adapter's dispatch contract;
                      this profile cannot add protocols, tools or destinations.
                    properties:
                      memoryBytes:
                        format: int64
                        maximum: 268435456
                        minimum: 1
                        type: integer
                      outputBytes:
                        format: int64
                        maximum: 65536
                        minimum: 1
                        type: integer
                      taskBytes:
                        format: int64
                        maximum: 2048
                        minimum: 1
                        type: integer
                      timeoutMillis:
                        format: int64
                        maximum: 300000
                        minimum: 1
                        type: integer
                      workspace:
                        enum:
                        - none
                        type: string
                    required:
                    - memoryBytes
                    - outputBytes
                    - taskBytes
                    - timeoutMillis
                    - workspace
                    type: object
                  mote:
                    properties:
                      hash:
                        maxLength: 71
                        pattern: ^blake3:[0-9a-f]{64}$
                        type: string
                    required:
                    - hash
                    type: object
                  platform:
                    enum:
                    - linux/amd64
                    type: string
                  publisherKey:
                    maxLength: 64
                    pattern: ^[0-9a-f]{64}$
                    type: string
                  revision:
                    maxLength: 64
                    minLength: 1
                    type: string
                  runtimeData:
                    description: Immutable data delivery has no supported contract
                      in this profile.
                    items:
                      properties:
                        hash:
                          maxLength: 71
                          pattern: ^blake3:[0-9a-f]{64}$
                          type: string
                      required:
                      - hash
                      type: object
                    maxItems: 0
                    type: array
                required:
                - closure
                - contractVersion
                - entryPoint
                - executable
                - lane
                - lifecycle
                - limits
                - mote
                - platform
                - publisherKey
                - revision
                type: object
                x-kubernetes-validations:
                - message: JSON loop ceilings must match the selected adapter contract
                  rule: 'self.contractVersion == ''celln.json-tools/v1'' ? has(self.json)
                    : !has(self.json)'
              cellnLimits:
                description: |-
                  CellnLimits may only attenuate numeric ceilings from CellnProfileRef. The
                  live resolver verifies it never raises a platform ceiling.
                properties:
                  memoryBytes:
                    format: int64
                    maximum: 268435456
                    minimum: 1
                    type: integer
                  outputBytes:
                    format: int64
                    maximum: 65536
                    minimum: 1
                    type: integer
                  taskBytes:
                    format: int64
                    maximum: 2048
                    minimum: 1
                    type: integer
                  timeoutMillis:
                    format: int64
                    maximum: 300000
                    minimum: 1
                    type: integer
                  workspace:
                    enum:
                    - none
                    type: string
                required:
                - memoryBytes
                - outputBytes
                - taskBytes
                - timeoutMillis
                - workspace
                type: object
              cellnProfileRef:
                description: |-
                  CellnProfileRef explicitly selects one cluster-scoped immutable platform
                  runtime revision. It never resolves through the namespaced AgentRuntime
                  namespace and cannot be combined with inline Celln executable authority.
                properties:
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                  revision:
                    maxLength: 64
                    minLength: 1
                    type: string
                required:
                - name
                - revision
                type: object
              conformance:
                description: |-
                  Conformance records this runtime's conformance status against the
                  versioned adapter contract.
                properties:
                  owner:
                    description: Owner identifies who performed the conformance assessment.
                    type: string
                  reference:
                    description: Reference is a URL to the conformance report or run.
                    type: string
                  status:
                    description: 'Status is one of: pending, conformant, non-conformant.'
                    enum:
                    - pending
                    - conformant
                    - non-conformant
                    type: string
                type: object
              contractVersion:
                description: |-
                  ContractVersion is the adapter contract version this image implements
                  (e.g. "v1alpha1"). Adapters must fail closed on versions they do not
                  understand.
                type: string
              image:
                description: |-
                  Image is the digest-pinned OCI reference that becomes the pod's primary
                  process. A mutable tag is rejected: the digest is the trust anchor, and
                  it is recorded on status.resolvedImageDigest for audit. It is required
                  for OCI runtimes and left empty for Celln-native runtimes.
                type: string
              model:
                description: |-
                  Model describes how this runtime routes to a model by default. Agents
                  that bind this runtime may still override it through their own config.
                properties:
                  authSecretRef:
                    description: |-
                      AuthSecretRef references a Secret key holding the provider credential.
                      Sympozium validates this reference against the backing Agent's allowlist
                      at run time; it does not trust it as written here.
                    type: string
                  baseURL:
                    description: BaseURL overrides the provider's default endpoint.
                    type: string
                  model:
                    description: Model is the model name the runtime calls.
                    type: string
                  provider:
                    description: |-
                      Provider is the model provider (e.g. "openai-compatible", "anthropic",
                      "deepseek").
                    type: string
                type: object
              resources:
                description: |-
                  Resources are the primary container's requests/limits. When nil, the
                  platform defaults apply.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              session:
                description: |-
                  Session describes the network contract exposed by a persistent-session
                  capable adapter. It is deliberately absent from v1alpha1 one-shot
                  runtimes: a runtime must opt in explicitly before Sympozium will keep it
                  running behind a Service.
                properties:
                  port:
                    description: Port is the container and Service port on which the
                      adapter listens.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  protocol:
                    description: |-
                      Protocol identifies the session wire protocol. The initial supported
                      value is "openai-chat".
                    enum:
                    - openai-chat
                    type: string
                required:
                - port
                - protocol
                type: object
              supportOwner:
                description: |-
                  SupportOwner identifies the team or person responsible for this runtime,
                  surfaced on run detail so operators know who to page.
                type: string
            required:
            - image
            type: object
            x-kubernetes-validations:
            - message: inline celln authority and cellnProfileRef are mutually exclusive
              rule: '!(has(self.celln) && has(self.cellnProfileRef))'
            - message: cellnLimits requires cellnProfileRef
              rule: '!has(self.cellnLimits) || has(self.cellnProfileRef)'
          status:
            description: AgentRuntimeStatus is the observed state of an AgentRuntime.
            properties:
              conditions:
                description: |-
                  Conditions represent the latest observations (Ready, and a reason when
                  the spec fails validation).
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              resolvedImageDigest:
                description: |-
                  ResolvedImageDigest is the digest parsed from spec.image. It is what a
                  run audit should record, independent of the exact reference syntax the
                  operator wrote.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
